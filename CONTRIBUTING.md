# Contributing

## Workflow

1. Create a branch from `main`.
2. Implement the change + tests.
3. Run `./gradlew test codenarcMain codenarcTest`.
4. Open a Pull Request with description and risks.

## Standards

- Public API in `vars/`.
- Logic in `src/`.
- Every bugfix must include a test.
s